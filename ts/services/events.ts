/*
 * Handle state-updates in response to events by delegating to other services. Handles
 * state-updates and rerenders.
 */

import { state } from "./config.ts";

export function onCodeEdit() {

}

export function onFileChange() {

}
